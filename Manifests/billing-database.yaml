# Service pour le billing-database
apiVersion: v1
kind: Service
metadata:
  name: billing-database-service
  namespace: default
  labels:
    app: billing-database
    component: service
spec:
  selector:
    app: billing-database
  ports:
  - name: postgres
    port: 5432
    targetPort: 5432
    protocol: TCP
  type: ClusterIP

---
# StatefulSet pour le billing-database
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: billing-database
  namespace: default
  labels:
    app: billing-database
    component: statefulset
spec:
  serviceName: "billing-database-service"
  replicas: 1
  selector:
    matchLabels:
      app: billing-database
  template:
    metadata:
      labels:
        app: billing-database
    spec:
      initContainers:
      - name: volume-permissions
        image: busybox
        command: ["sh", "-c", "chown -R 101:102 /var/lib/postgresql/data && chmod 700 /var/lib/postgresql/data"]
        volumeMounts:
        - name: billing-db-storage
          mountPath: /var/lib/postgresql/data
        securityContext:
          runAsUser: 0
      containers:
      - name: billing-database
        image: sniang96/billing-database:orchestrator
        securityContext:
          runAsUser: 101    # postgres user ID in Debian
          runAsGroup: 102   # postgres group ID in Debian
        ports:
        - containerPort: 5432
        volumeMounts:
        - name: billing-db-storage
          mountPath: /var/lib/postgresql/data
        env:
        - name: POSTGRES_USER
          valueFrom:
            secretKeyRef:
              name: billing-db-credentials
              key: POSTGRES_USER
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: billing-db-credentials
              key: POSTGRES_PASSWORD
        - name: POSTGRES_DB
          valueFrom:
            configMapKeyRef:
              name: billing-db-config
              key: POSTGRES_DB
        - name: BILLING_DB
          valueFrom:
            configMapKeyRef:
              name: billing-db-config
              key: BILLING_DB
        - name: POSTGRES_PORT
          valueFrom:
            configMapKeyRef:
              name: billing-db-config
              key: POSTGRES_PORT
        - name: BILLING_DB_HOST
          valueFrom:
            configMapKeyRef:
              name: billing-db-config
              key: BILLING_DB_HOST
        livenessProbe:
          exec:
            command:
              - pg_isready
              - "-U"
              - "$(POSTGRES_USER)"
          failureThreshold: 10
          periodSeconds: 10
          initialDelaySeconds: 30
        readinessProbe:
          exec:
            command:
              - pg_isready
              - "-U"
              - "$(POSTGRES_USER)"
          initialDelaySeconds: 60
          periodSeconds: 10
        startupProbe:
          exec:
            command:
              - pg_isready
              - "-U"
              - "$(POSTGRES_USER)"
          failureThreshold: 40
          periodSeconds: 10
  volumeClaimTemplates:
  - metadata:
      name: billing-db-storage
      namespace: default
      labels:
        app: billing-database
        component: storage
    spec:
      accessModes: ["ReadWriteOnce"]
      resources:
        requests:
          storage: 200Mi
      storageClassName: "local-path"