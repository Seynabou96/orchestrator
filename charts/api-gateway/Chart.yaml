apiVersion: v2
name: api-gateway
description: >
  Chart Helm pour api-gateway, namespace "api-gateway". Seul point
  d'entrée externe du cluster — appelle inventory-app (namespace
  inventory) et rabbitmq (namespace billing) en cross-namespace.
  Fait partie du projet Orchestrator de Seynabou Niang.
type: application
version: 1.0.0
appVersion: "1.0.0"
